import { type CardRoleProps } from "@/types";
import styles from "./CardMember.module.scss";

export default function CardMember({ role }: CardRoleProps): JSX.Element {
  return <div className={styles.wrapper}>test</div>;
}
