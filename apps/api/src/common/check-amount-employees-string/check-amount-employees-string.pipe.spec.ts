import { CheckAmountEmployeesStringPipe } from "./check-amount-employees-string.pipe";

describe("CheckAmountEmployeesStringPipe", () => {
  it("should be defined", () => {
    expect(new CheckAmountEmployeesStringPipe()).toBeDefined();
  });
});
